<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph.xml"
    app:startDestination="@id/news_fragment">

    <fragment
        android:id="@+id/news_fragment"
        android:name="com.hisham.news.ui.ui.home.learn.feed.NewsFragment"
        android:label="Feed"
        tools:layout="@layout/news_fragment">
        <action
            android:id="@+id/action_news_fragment_to_news_detail_fragment"
            app:destination="@id/news_detail_fragment"
             />
    </fragment>

    <fragment
        android:id="@+id/news_detail_fragment"
        android:name="com.hisham.news.ui.ui.home.learn.feed.NewsDetailFragment"
        android:label="Detail"
        tools:layout="@layout/news_detail_fragement"/>

</navigation>
